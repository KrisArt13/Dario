// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    base: '/eldariportaf',
    outDir: './dist' // Asegurar que la salida esté en "dist"
  });